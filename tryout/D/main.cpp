#include <iostream>
#include <vector>

using namespace std;

int arr[30001];

vector<int> v;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	string s;
	int n;
	cin>>n>>s;
	for(int i = 0;i<n;i++) {
		cin>>arr[i];
	}
	for(!v.empty()) {
		
	}

}